@page
@model IndexModel
@{
    ViewData["Title"] = "last-fm-not-mine-alert-web";
}
<h2>@ViewData["Title"]</h2>

<p>
    @using(Html.BeginForm())
    {
        @Html.Label("Enter artist name:")
        @Html.TextBox("ArtistName")
        <input type="submit" value="Add new artist" />
    }
</p>
